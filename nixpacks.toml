[phases.setup]
nixPkgs = [
  "openssl", 
  "pkg-config", 
  "python3", 
  "gcc", 
  "libgcc", 
  "gnumake", 
  "dpkg",
  "nodejs_21",
  "yarn"
  ]

[phases.install]
cmds = [
  "yarn",
  ]

[phases.build]
cmds = [
  "yarn workspace @calcom/prisma db-deploy",
  "yarn build",
  ]